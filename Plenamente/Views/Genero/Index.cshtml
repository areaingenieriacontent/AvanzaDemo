@model IEnumerable<Plenamente.Models.Genero>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Gene_Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gene_Registro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Gene_Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gene_Registro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Gene_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Gene_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Gene_Id })
        </td>
    </tr>
}

</table>
