@model IEnumerable<Plenamente.Models.Afp>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Afp_Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Afp_Registro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Afp_Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Afp_Registro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Afp_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Afp_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Afp_Id })
        </td>
    </tr>
}

</table>
