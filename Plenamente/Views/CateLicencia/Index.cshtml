@model IEnumerable<Plenamente.Models.CateLicencia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cate_Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cate_Registro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cate_Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cate_Registro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cate_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cate_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cate_Id })
        </td>
    </tr>
}

</table>
