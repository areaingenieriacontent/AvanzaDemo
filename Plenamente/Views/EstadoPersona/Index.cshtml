@model IEnumerable<Plenamente.Models.EstadoPersona>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Espe_Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Espe_Registro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Espe_Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Espe_Registro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Espe_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Espe_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Espe_Id })
        </td>
    </tr>
}

</table>
